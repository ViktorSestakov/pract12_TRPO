<Page x:Class="pract12_TRPO.Pages.UserFormPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:pract12_TRPO.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserFormPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="59*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин" FontSize="30"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Имя" FontSize="30"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Эл. почта" FontSize="30"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Пароль" FontSize="30"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Дата создания" FontSize="30"/>

        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Login, ValidatesOnDataErrors=True}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.ColumnSpan="2"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.ColumnSpan="2"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Email, ValidatesOnDataErrors=True}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.ColumnSpan="2"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Password, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.ColumnSpan="2"/>
        <DatePicker Grid.Row="4" Grid.Column="1" SelectedDate="{Binding CreatedAt}" Style="{StaticResource MaterialDesignDatePicker}" IsEnabled="False" Grid.ColumnSpan="2"/>

        <StackPanel Grid.Column="1" Grid.Row="5" Orientation="Vertical" Grid.ColumnSpan="2">
            <Button Click="Save" Margin="0 0 0 5" Content="Сохранить" Width="200" Style="{StaticResource MaterialDesignRaisedButton}"/>
            <Button Click="Back" Content="Назад" Width="200" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Page>
